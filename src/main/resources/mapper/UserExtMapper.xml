<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yichen.community.mapper.UserExtMapper">
    <resultMap id="BaseResultMap" type="com.yichen.community.model.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Sep 10 06:06:12 CST 2019.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="account_id" jdbcType="VARCHAR" property="accountId" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="token" jdbcType="CHAR" property="token" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="bio" jdbcType="VARCHAR" property="bio" />
        <result column="avatar_url" jdbcType="VARCHAR" property="avatarUrl" />
    </resultMap>


    <select id="getEncodePasswordByUsername" resultType="java.lang.String">
        select password from user where name = #{username}
    </select>

    <insert id="register" >
        insert into user (name, password, gmt_create, salt, account_type, email, phone, avatar_url, token) values (#{name}, #{password}, #{gmtCreate}, #{salt}, #{accountType}, #{email}, #{phone}, #{avatarUrl}, #{token})
    </insert>

    <select id="isExist" resultType="java.lang.Integer">
        select count(*) from user
        <where>
            <if test="username != null">
                name = #{username}
            </if>
            <if test="email != null">
                email = #{email}
            </if>
            <if test="phone != null">
                phone = #{phone}
            </if>
        </where>

    </select>
</mapper>